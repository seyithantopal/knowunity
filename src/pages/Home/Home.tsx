import React, { FC } from 'react';

// Styles
import { Wrapper } from './styles';

const Home: FC = () => {
  return (
    <Wrapper>
      <div>Home Page</div>
    </Wrapper>
  );
};

export default Home;
