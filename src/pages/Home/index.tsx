import React, { FC } from 'react';

// Styles
import { Wrapper } from './styles';

// Components
import Lists from '../../components/Lists';

const Home: FC = () => {
  return (
    <Wrapper>
      <Lists />
    </Wrapper>
  );
};

export default Home;
