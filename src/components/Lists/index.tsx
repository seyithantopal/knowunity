import React, { FC } from 'react';

// Styles
import { Wrapper, Title, Divider } from './styles';

const Lists: FC = () => {
  return (
    <Wrapper>
      <Title>Lists</Title>
      <Divider />
    </Wrapper>
  );
};

export default Lists;
